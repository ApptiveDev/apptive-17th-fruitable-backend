spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/fruitable
    username: sa
    password:
    driver-class-name: org.h2.Driver
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 100MB
      enabled: true

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        #show_sql: true
logging:
  level:
    org.hibernate.sql: debug
#   org.hibernate.type: trace

img:
  location: /Users/hjhwang/Projects/Spring/fruitable/img #상품 이미지 업로드 경로
  uploadPath: file:///Users/hjhwang/Projects/Spring/ #리소스 업로드 경로